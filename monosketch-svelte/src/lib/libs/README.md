`libs` is a collection of common data structures and algorithms that are used throughout the project
but does not belong to any specific module.

This can be extracted as a separate library in the future.
